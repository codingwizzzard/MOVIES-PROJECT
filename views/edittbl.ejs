<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ZEE5 - Edit Table</title>
    <!-- Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <!-- fontawesome cdn -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" />
    <!-- css -->
     <link rel="stylesheet" href="../assets/css/edittbl.css">
</head>

<body>
    <!-- Header starts -->
    <%- include('./partials/header.ejs') %>
        <!-- Header ends -->
        <main>
            <caption>
                <h1 class="text-center my-5 text-light">Movie List</h1>
            </caption>
            <table border="1" class="table text-center">
                <tr>
                    <th>Movie Name</th>
                    <th>Movie Desc</th>
                    <th>Timestamp</th>
                    <th>Rating</th>
                    <th>Image</th>
                    <th>Genre</th>
                    <th>Action</th>
                </tr>
                <% data.map(val=> { %>
                    <tr>
                        <td>
                            <%= val.name %>
                        </td>
                        <td>
                            <%= val.description %>
                        </td>
                        <td>
                            <%= val.time %>
                        </td>
                        <td>
                            <%= val.rating %>
                        </td>
                        <td><img src="<%= val.image %>" alt="image" width="100px"></td>
                        <td>
                            <%= val.genre %>
                        </td>
                        <td>
                            <a href="/deleteData/<%= val._id %>" class="btn btn-danger">
                                <i class="fa-solid fa-trash"></i>
                            </a>
                            <a href="/editData/<%= val._id %>" class="btn btn-primary">
                                <i class="fa-regular fa-pen-to-square"></i>
                            </a>
                        </td>
                    </tr>
                    <% }) %>
            </table>
        </main>
        <!-- Bootstrap JS -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>