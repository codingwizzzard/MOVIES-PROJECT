<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ZEE5 - Home Page</title>
  <!-- Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <!-- Bootstrap CSS -->
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <!-- css -->
  <link rel="stylesheet" href="../assets/css/index.css">
</head>

<body>

  <%- include('./partials/header.ejs') %>
    <div class="container">
      <div class="row">
        <% data.map((val)=> { %>
          <div class="col-md-4">
            <div class="card">
              <img class="card-img-top" src="<%= val.image %>" alt="Movie Poster">
              <div class="card-body">
                <h5 class="card-title">
                  <%= val.name %>
                </h5>
                <p class="card-text fw-bold">
                  <%= val.description %>
                </p>
                <p class="card-text">
                  <%= val.time %> • <%= val.genre %>
                </p>
                <p class="card-text">Rating: <%= val.rating %>
                </p>
                <a href="#" class="btn btn-primary btn-block">Watch Now</a>
                <a href="#" class="btn btn-secondary btn-block">Share</a>
              </div>
            </div>
          </div>
          <% }) %>
      </div>
    </div>
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>